#include <stdio.h>

/**
 * main - entry point that prints all single digit numbers of base 10
 *
 * printf - output
 * return: prints 0 if successful else 1
 */


int main(void)
{

int num;

	for (num = 0; num <= 10; num++)
		{
		printf("%d", num);
		}
printf("\n");
return (0);

}
